# Place authoritative guideline PDFs here (NOT committed — see .gitignore).
#
# MVP priority list:
#   1. Malawi Standard Treatment Guidelines (MSTG)
#   2. Malawi malaria treatment guidelines
#   3. Malawi integrated HIV guidelines
#   4. Malawi TB guidelines
#   5. One additional high-value guideline
#
# Record provenance for every file added (source institution + URL + date accessed).
# Ingest with:
#   python scripts/ingest.py data/guidelines/<file>.pdf --doc-id <id> --title "<title>" --edition "<ed>" --year <yyyy> --source "Malawi Ministry of Health"
